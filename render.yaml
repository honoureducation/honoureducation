services:
  # Backend API Service
  - type: web
    name: academic-excellence-api
    env: node
    buildCommand: cd backend && npm install
    startCommand: cd backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: MONGODB_URI
        value: mongodb+srv://future_db_user:aJUNXbMMaU37P43D@cluster0.mw1hrof.mongodb.net/student_assessment?retryWrites=true&w=majority&appName=Cluster0

  # Frontend Static Site
  - type: web
    name: academic-excellence-frontend
    env: static
    buildCommand: cd frontend && npm install && npm run build
    staticPublishPath: frontend/build
    envVars:
      - key: REACT_APP_API_URL
        value: https://honoureducation.onrender.com